apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-B.fullname" . }}-test-cfdsfsfsfsfsfsfjjofdsdfsdfsfsfsdfsdnnection"
  labels:
    {{- include "service-B.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-B.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
