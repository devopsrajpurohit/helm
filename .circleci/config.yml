version: 2.1
orbs:
  helm: banzaicloud/helm@0.0.8
workflows:
  helm-chart-release:
    jobs:
      - helm/publish-chart:
          context: helm
          charts-dir: charts
          add-extra-repositories:
            - run: "helm repo add extra \thttps://devopsrajpurohit.github.io/helm/"
         